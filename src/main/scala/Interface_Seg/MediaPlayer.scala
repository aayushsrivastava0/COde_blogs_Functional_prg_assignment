package Interface_Seg

object MediaPlayer extends App{
  val VlcMediaPlayer = () => {
    "Play Audio & Video using Media player"

  }
  println(VlcMediaPlayer())

  val DivMediaPlayer = () => {
    "Play Audio & Video using Media player"
  }
  println(DivMediaPlayer())

  val WinAmpPlayer = () => {
    "Play Audio on WinAmp"
  }
  println(WinAmpPlayer())
}
